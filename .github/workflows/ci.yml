name: CI
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
    inputs:
      force_heavy:
        description: Run the macOS and Windows build jobs regardless of changed paths
        required: true
        default: true
        type: boolean
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  ci:
    uses: EsionHsrahLatigid/yup-actions/.github/workflows/plugin-ci.yml@e6357cc6fb863b3691a9fe3203c54fe74bdec74c
    with:
      product_name: ZeroJet
      product_slug: zerojet
      cmake_option_prefix: ZEROJET
      debug_targets_json: '["zerojet_engine_tests","zerojet_plugin_bridge_tests","zerojet_standalone_bridge_tests"]'
      release_test_targets_json: '["zerojet_engine_tests","zerojet_plugin_bridge_tests","zerojet_standalone_bridge_tests"]'
      windows_debug_plugins: true
